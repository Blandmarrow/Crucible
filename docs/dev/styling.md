# Styling: the Tailwind/CSS design system, the brand mark & shared dialogs

This file covers the frontend's visual layer plus the shared dialog layer: CSS custom-property tokens, the reusable component classes in `@layer components`, the `CrucibleMark` brand mark and its export/drift constraints, `ConfirmDialog`, and the `useModalBehavior` hook every modal spreads. Component and state conventions live in `docs/dev/frontend-core.md`.

### Design tokens and component classes

Tailwind CSS v3 with a dark theme. Color tokens are CSS custom properties defined in `index.css` (`:root { --bg, --surface-1/2/3, --accent, --line, --fg, --warn, --bad, --info }`) and aliased in `tailwind.config.js` so they can be used as Tailwind classes. Geist/Geist Mono fonts are loaded via Google Fonts in `index.html`. Reusable component classes are defined in `frontend/src/index.css` under `@layer components`:

| Class | Purpose |
|---|---|
| `.btn` + variants — both compound (`.btn.primary`, `.btn.ghost`, `.btn.danger`, `.btn.sm`) and hyphenated (`.btn-primary`, `.btn-secondary`, `.btn-ghost`, `.btn-danger`, `.btn-sm`) forms are defined; the hyphenated forms are the more common outside the comfy components | Button variants |
| `.link-btn` | Action that reads as a link inside a sentence — inherits `font: inherit` rather than being a `.btn` variant, so it sits on the embedding line's baseline. The gallery's select-all-matching offer |
| `.input`, `.select`, `.checkbox` | Form controls |
| `.panel`, `.panel-h`, `.panel-b` | Card container with header/body sections |
| `.form-row` | 2-col grid (200px label + 1fr control) used in CaptioningPage and ExportPage |
| `.model-row` | Radio-style model selector row with name, description, and VRAM label |
| `.stat-card` | Metric card with large value, label, and optional delta |
| `.hist` / `.hist-axis` | CSS grid bar chart; set `--cols` and `gridTemplateRows: "1fr"` inline; bars use percentage `height` |
| `.flag-card` | 3-col grid (icon, label/desc, count) for quality flags |
| `.badge`, `.badge.dot`, `.badge.good/warn/bad/info/solid` | Semantic badge variants |
| `.icon-btn` | 30×30 ghost icon button |
| `.sel-bar` | Sticky bottom pill bar for selection actions |
| `.crumbs` | Breadcrumb navigation |
| `.nav-section`, `.nav-tail` | Sidebar section header and count badge |
| `.tabs`, `.tab` | Tab bar with accent underline active state. **Wraps** (`flex-wrap: wrap`) — Settings carries nine tabs and Bulk Edit eight, and either strip spilled past its own underline in a split pane. A page whose tab strip has grown wants its `maxWidth` checked too: Settings sits at 980 and Bulk Edit at 900 for exactly that reason, and anything beside the strip (Bulk Edit's *Reset to defaults*) belongs in the page header, not next to two rows of tabs |
| `.dialog-bg`, `.dialog` | Fixed full-screen dimmer (z-index 60) + centered modal panel (max-width 420px). Used by the `DatasetsPage` modals; `ConfirmDialog` predates these and uses its own Tailwind utilities instead |
| `.ds-flash` | 2s accent `box-shadow` pulse marking a newly created dataset (`box-shadow`, not `border`, so nothing reflows) |

### The brand mark

**`CrucibleMark`** (`frontend/src/components/common/CrucibleMark.tsx`) — the brand mark: a 5×5 contact sheet that curates down to a C. Props: `size` (px, default 22), `animated`, `className`, `label`. Renders 25 rounded rects on a 64-unit viewBox; the `KEPT` set is the C, `BRIGHT` the four `--accent-2` cells, everything else the dropped grid at `--accent-dim`. Used at 22px in the Sidebar brand and 132px (animated) in the TopBar restart overlay. The **component** is token-driven — no hardcoded hex — so palette changes carry through automatically; both current callsites are decorative so the mark defaults to `aria-hidden`, and a callsite that stands alone should pass `label` to get `role="img"` + `aria-label`. The **app icon is not token-driven**: `frontend/public/favicon.svg` (a copy of `docs/images/crucible-icon.svg`) and the favicon/apple-touch PNGs hardcode the palette, because a standalone icon file is never in the document's CSS scope. On a palette change these must be re-exported by the designer and re-copied into `frontend/public/` — the CSS token change alone will not touch them.

The designer's exports are the source of truth for the geometry and live in `docs/images/` (`crucible-mark.svg` static, `crucible-icon.svg` the app-icon variant, `Crucible Logo Animated.html` animated, `icons/*.png` rasters, `README.txt` brand notes). **If you change the mark, change it there and in the component** — they are independent transcriptions of the same design and will silently diverge. `scripts/check_mark.py` diffs the component against the exports (cell roles, bright cells, per-cell animation variant, and the app-icon C shape) and byte-compares the `frontend/public/` favicons against their `docs/images/` sources, failing on any drift. It runs in CI (`.github/workflows/check-mark.yml`).

The launcher's startup splash is the one copy that is **not** a transcription: `scripts/splash_server.py` substitutes `Crucible Logo Animated.html`'s keyframes and `<svg>` into `scripts/splash.html` when it serves the page, so it cannot drift by construction. What can break is the substitution itself — a renamed export, a reworked `<style>` block, a placeholder edited out of the template — which fails silently as a splash with no mark on it, so `check_mark.py` renders the page through `build_page()` and diffs that. See `docs/dev/environment-setup.md` (§ Startup splash).

**Class names in `@layer components` must appear verbatim in the source.** Tailwind only emits a rule from `@layer components` if its selector is found as literal text when scanning `content` files, so a dynamically assembled class name (``className={`cm-keep-${variant}`}``) type-checks, renders, and is then **purged from the bundle** — the element carries a class that no CSS matches. This fails silently and only in a production build; the symptom is an element rendering unstyled with no error. Always write the full class name out (`variant === "a" ? "cm-keep-a" : "cm-keep-b"`), as `CrucibleMark` and `.pp-fill-indeterminate` do. `scripts/check_mark.py` asserts the built CSS still contains the mark's rules when `frontend/dist` is present.

`animated` attaches the `.cm-keep-a/b` and `.cm-drop-a/b` classes whose keyframes live in `index.css`. One 9.2s cycle: the grid flickers as if sampling, non-C cells shrink to dots and return, then vanish, leaving the C. The A/B split is a checkerboard (`(indexOf(x) + indexOf(y)) % 2`) so neighbouring cells fire on offset timelines. A `prefers-reduced-motion: reduce` block resolves the mark to its static form rather than freezing it mid-cycle.

**`--accent-dim` (`#0f6e4e`)** exists specifically for the mark's unresolved grid cells. It is *not* interchangeable with `--accent-deep` (`#064e3b`): at `--accent-deep` the dropped cells are dark enough that the C is already legible on frame one, which spoils the animation's reveal. Static cells use `--accent-dim` at `opacity .09` — the value the animation resolves to, and a near-exact match for `--accent-deep` once composited on `--bg`, which is why the static mark looks right either way and the difference only shows in motion.

### ConfirmDialog

**`ConfirmDialog`** (`frontend/src/components/common/ConfirmDialog.tsx`) — shared modal for destructive confirmations. It takes its dialog role, focus restore and Escape-closes behavior from `useModalBehavior` (§ Modal behavior below), passing `autoFocus: false` so the focus rules below stay authoritative, and never enabling backdrop-click close — a destructive confirm must be answered, not dismissed by a stray click. Escape maps to Cancel. Keyboard-aware: auto-focuses Cancel on mount by default (safe default for destructive actions), ArrowLeft/ArrowRight switch focus between Cancel and the confirm button, Enter fires the focused button natively. When adding any global `keydown` listener that handles ArrowLeft/ArrowRight, suppress it while a `ConfirmDialog` is open to avoid background navigation competing with dialog focus — see `showDeleteConfirm` guard in `ImageDetailPage`'s arrow-key effect. Accepts an optional `defaultFocus?: "cancel" | "confirm"` prop to override the focused button per-callsite. When `danger=true` and no `defaultFocus` is provided, the component reads `localStorage.getItem(CONFIRM_DEFAULT_KEY)` (from `constants/storage.ts` — see `docs/dev/persistence.md`) to respect the user's preference set in Settings → UI Behavior.

One sanctioned exception to the focus-Cancel rule: `DeleteBranchPrompt` (in `frontend/src/components/versioning/BranchSelector.tsx`) is destructive but keeps `useModalBehavior`'s default `autoFocus: true`, landing focus on its branch `<select>`. Choosing the branch *is* the dialog's task, and a `<select>` cannot be Enter'd into a deletion — the confirm button is a separate stop away. Do not "fix" this to match `ConfirmDialog`.

### Modal behavior

**`useModalBehavior`** (`frontend/src/hooks/useModalBehavior.ts`) — the single source of modal keyboard/ARIA behavior. Returns `{ overlayProps, panelProps }` to spread onto the overlay and the panel `<div>`; it adds no markup and no styles, which is why it is a hook and not a wrapper — the modals span Tailwind overlays, inline-styled panels and three z-index tiers. Options: `onClose`, `label` (accessible name), `closeOnBackdrop` (default off), `autoFocus` (default on). It gives the panel `role="dialog"`, `aria-modal`, `tabIndex={-1}`, restores focus to whatever opened the modal (unless that element is gone, as after a delete), and handles Escape and Tab/Shift+Tab cycling (focusables re-queried per keydown).

Four details are load-bearing. The handlers sit on the **panel**, not on `window`, so a stacked modal's Escape closes only the top one and the one underneath stays inert; Escape also calls `stopPropagation()`, which keeps a page's window-level Escape handler from firing behind an open dialog. The overlay's `onMouseDown` cancels the default focus change when the press lands on the backdrop — otherwise that click blurs the panel and Escape silently stops working. A panel handler cannot fire once focus is lost to `<body>`, which is what a focused control unmounting does (closing `DirPickerModal`'s inline new-folder row), so a narrow `document`-level Escape fallback covers it, gated on *nothing* being focused and on this panel being the last `[role="dialog"]` in the document — that gate is what keeps stacking correct. And the mount-focus target is resolved once into a ref: React applies `autoFocus` during commit, before effects run, so the hook moves focus only when nothing inside the panel has it, and the ref keeps StrictMode's dev-only remount from landing focus elsewhere.

Adopted by all 17 shared modals in `common/`, `gallery/`, `versioning/`, `comfy/` and `video/` (the newest being `frontend/src/components/gallery/MoveSubfolderModal.tsx`, which could adopt it precisely because it shipped as its own component) (the fifth directory, adding `ExtractFramesModal` and `ReextractFramesModal`), plus `BranchSelector`'s two inline dialogs; elsewhere it is a spread on the existing divs, so never hand-roll Escape or focus handling again. Enable `closeOnBackdrop` only where that affordance already existed, never on a destructive confirm; give any nested control with its own Escape (`DirPickerModal`'s new-folder row) a `stopPropagation()`. A dialog rendered inline inside a long-lived parent has to become its own component first: the hook mounts with the dialog, so calling it in a parent that stays mounted traps focus permanently. `SelectionToolbar` is where both halves of that are visible — nine of its overlays are still bare `fixed inset-0` divs with no hook, while the four it renders as extracted components (`ConfirmDialog`, `SetProvenanceModal`, `MoveToDatasetModal`, `ReextractFramesModal`) do use it, which is exactly what extraction buys. The page-level video overlays on `VideoDetailPage`/`ImageDetailPage` are already extracted and already hook users. `frontend/e2e/modal-a11y.spec.ts` covers Escape, stacking, focus restore and the backdrop rules.

### Context menu

**`ContextMenu`** (`frontend/src/components/common/ContextMenu.tsx`) — a pointer-anchored menu, `position: fixed` at `(x, y)`. Props `{x, y, actions, onClose}`; an action is `{label, icon?, onClick, danger?}` and `icon` is optional, so a menu whose entries have no natural glyph renders without blank slots. Two call sites: `FileBrowserPage`'s entry rows (`docs/dev/file-browser.md`) and `GalleryPage`'s subfolder rows (`docs/dev/gallery.md` § The subfolder row context menu). Import it; never re-inline it.

It deliberately does **not** use `useModalBehavior`. That hook is modal-shaped — focus trap, `role="dialog"`, `aria-modal` — and a context menu is not a dialog. So Escape-to-close and outside-click-to-close live in this component instead, which is its answer to the never-hand-roll-Escape rule above: one implementation, not one per call site. Escape `stopPropagation()`s, so dismissing the menu does not also reach a page's own document-level Escape handler. A `useLayoutEffect` measures `getBoundingClientRect()` and clamps into the viewport (8 px pad) — required rather than cosmetic, since the gallery sidebar is 180 px at the left edge and a row near the bottom of a long tree would otherwise push the menu off-screen. The container carries `role="menu"` + `aria-orientation`, each button `role="menuitem"`.

Not implemented on purpose: arrow-key roving focus, portaling, submenus. The **lookalikes** anchored to a button rather than to a pointer are not this component and must not be converted to it by reflex — they share the behaviour, not the positioning, and that half lives in `usePopover` below.

### Anchored popovers

**`usePopover`** (`frontend/src/hooks/usePopover.ts`) — the non-modal sibling of `useModalBehavior`, for a panel anchored to a trigger button. Returns `{open, setOpen, anchorRef, triggerRef}`. `anchorRef` goes on the `position: relative` wrapper holding **both** the trigger and the panel, because outside-click is measured against it and a click inside the panel must not count as outside; `triggerRef` exists only so Escape can hand focus back rather than dropping it on `<body>`. Escape `stopPropagation()`s, for the same reason `ContextMenu` does: `GalleryPage` and `ImageDetailPage` both carry window-level Escape handlers that would otherwise clear the selection or close the pane behind the panel being dismissed. Three call sites: `GalleryPage`'s select-all caret menu, `LabelPicker`, and `LabelsPanel`'s per-row colour swatch in Settings. Same non-goals as `ContextMenu` — no roving focus, no portaling, no viewport clamping.

**`LabelPicker`** (`frontend/src/components/common/LabelPicker.tsx`) — two exports over the label vocabulary, and the answer to "one control per label does not scale". `LabelCheckList` is the body: a search box, a 240 px scrolling list, one native `<input type="checkbox">` per label with a colour dot and an optional count, and a `footer` slot the call site fills with its own semantics. `LabelPicker` wraps it in a `btn ghost sm` disclosure button (`triggerContent`/`triggerAriaLabel`/`active`). It knows nothing about filters or requests.

**`placement` is dictated by scroll ancestors, not by taste.** `"popover"` floats the panel absolutely and is correct only where nothing clips it — the gallery toolbar sets no `overflow`, which the select-all menu already proves. `"inline"` renders it in flow, and is required inside an `overflow-y: auto` container, where an absolute panel is cut off at the container's edge: the image detail rail and the Export page body are both such containers. The bulk modal skips the trigger and renders `LabelCheckList` directly, since the two lists *are* its content. Keyboard support is deliberately minimal — native checkboxes give Tab/Space and the focus ring, the search input autofocuses, Escape closes and returns focus, and `Enter` in the search box is `preventDefault`ed. No arrow-key roving, for the reason above; a checkbox list is not a `listbox`, so plain form controls are the correct ARIA shape. `utils/keyboard.ts::isTextEntryTarget` already covers `INPUT`, so the detail view's label hotkeys cannot fire into the search box.

### CSS hist bars

The `.hist` class sets `display: grid; align-items: end; height: 90px`. For percentage `height` on bar children to resolve, you must also set `gridTemplateRows: "1fr"` as an inline style on the `.hist` div. Without this the single implicit row has no definite height and percentage heights collapse to 0.
