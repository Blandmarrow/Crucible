# Postmortem index

This file indexes past incidents as one-line rows so code reviews and bug investigations
can check new code against failure classes we have already been burned by. Each real
incident gets a detail file under `docs/dev/postmortems/` (copy
`docs/dev/postmortems/PM-000-template.md` to `PM-NNN-short-slug.md`, next free number).

**Usage note**: treat LIVE and MITIGATED entries as an active checklist for their code
class. STRUCTURAL entries are kept for history only — a refactor made that class of bug
impossible — and should not drive review attention. Keep the Symptom column greppable:
phrase it with the words someone would actually search for when the bug resurfaces.

| ID | Symptom | Root-cause category | Status | Detail |
|---|---|---|---|---|
| PM-001 | Restore failed: UNIQUE constraint failed: images.dataset_id, images.filename (filename swaps/renumber chains, deleted-name reuse) | Batch write permuting values under a unique constraint without staged updates (SQLite checks constraints per statement) | MITIGATED | `docs/dev/postmortems/PM-001-restore-filename-collision.md` |
| PM-002 | Pre-restore/checkout auto-snapshot appeared on "main" and moved main's head while working on another branch | Auto-created record resolved context from a hardcoded name instead of current state | MITIGATED | `docs/dev/postmortems/PM-002-auto-snapshot-wrong-branch.md` |
| PM-003 | Restore crashed with PK IntegrityError / snapshot couldn't materialize an image after batch move to another dataset | Scope-removal path (move) bypassed the versioning deletion hook keyed on DB deletes only | MITIGATED | `docs/dev/postmortems/PM-003-move-bypassed-versioning-hook.md` |
| PM-004 | Setup printed "CUDA-enabled PyTorch installed" but `torch.cuda.is_available()` stayed False (`2.12.1+cpu`); ML silently ran on CPU; SAM3 `ModuleNotFoundError: No module named 'triton'` on Windows | Capability claimed from a package manager's exit code; `pip install "pkg>=X"` is a no-op when a wrong-variant build already satisfies the specifier | MITIGATED | `docs/dev/postmortems/PM-004-silent-cpu-torch-fallback.md` |
| PM-005 | ComfyUI generation run failed with `ValueError: too many values to unpack (expected 2)`; row wedged at "running", output files orphaned | A helper's positional tuple return was widened; a second caller still unpacked the old arity, and the narrow `except` list skipped the cleanup handler | MITIGATED | `docs/dev/postmortems/PM-005-tuple-return-widened-broke-caller.md` |
| PM-006 | `CREDITS.md` contained a license section nobody exported; `licenses.csv` cell executed as a spreadsheet formula; an image's provenance 422'd on every save | Scraped/EXIF strings interpolated into a generated document without escaping for its syntax, and captured without a length cap | MITIGATED | `docs/dev/postmortems/PM-006-untrusted-strings-forged-credits-md.md` |
| PM-007 | Two rescanned files sharing a stem (`a.png`/`a.jpg`, `clip.mp4`/`clip.mkv`) shared one thumbnail/poster — grid showed the same picture twice, deleting one video unlinked the other's poster | A path that *adopts* a filename off disk rather than picking one never acquired the stem-collision guard every name-picking path has | MITIGATED | `docs/dev/postmortems/PM-007-rescan-adopted-filenames-clobbered-thumbnails.md` |
| PM-008 | Extracted frames did not appear in the gallery until the job finished; TopBar pill counted to `94,102 / 173,318` then snapped back to `8 / 384` | One progress counter meaning two different things across a job's phases, defeating a consumer's monotonic high-water-mark gate | MITIGATED | `docs/dev/postmortems/PM-008-video-extract-progress-counter.md` |
| PM-009 | Replace-mode LUT or upscale (incl. crop+upscale) on a `.bmp`/`.gif`/`.tiff`/`.avif` left two files and one row — the served image never changed, the original stayed on disk, the thumbnail showed the new pixels; copy-mode upscale crashed the job on a path that was never written | A helper corrected a path the caller supplied (PNG fallback in `normalize_image_format`); one consumer followed the correction, its neighbour did not | MITIGATED | `docs/dev/postmortems/PM-009-lut-replace-stranded-the-row.md` |
| PM-010 | A frame cropped in place via `POST /images/{id}/crop` came back from the pass-2 preview as *eligible*, so re-extraction would have silently discarded the crop | First real consumer added to a column that until then only recorded history, with no single writer and five producers that never wrote it. `utils.record_in_place` is now that writer, with an AST guard in CI | MITIGATED | `docs/dev/postmortems/PM-010-in-place-overwrites-skipped-processing-history.md` |
| PM-011 | `POST /filesystem/move` (API-only; no UI caller) returned 200 but left the moved file's `Image`/`Video` row pointing at the old path — wrong dataset, 404 from `/file`, re-registered by the next rescan | A path predicate (`src.is_file()` / `src.is_dir()`) evaluated *after* the `shutil.move` that invalidates it, so an `if/elif` with no `else` silently skipped the whole DB-sync block | MITIGATED | `docs/dev/postmortems/PM-011-move-classified-the-source-after-moving-it.md` |
| PM-012 | Gallery forgot its page: opening an image from page 2 and clicking Back landed on page 1, and the remembered page/scroll was then overwritten with 1 | A debounced input effect also fires on **mount**, so its `setPage(1)` reset discarded the page restored from `localStorage` 350 ms after arrival | MITIGATED | `docs/dev/postmortems/PM-012-mount-debounce-reset-restored-gallery-page.md` |
| PM-013 | Re-extract/LUT/upscale to a different extension left two files one row and a **404** from `GET /images/{id}/file`; same-suffix runs kept stale geometry and lost the `processing_history` entry | A fallible step (`generate_thumbnail`, `unlink`) between an irreversible file write and its `commit()`: the row rolled back, the filesystem did not. Amended three times; Tier 2 is now empty, the last two being `images.py`'s single crop and the single **resize** the original sweep never enumerated | MITIGATED | `docs/dev/postmortems/PM-013-fs-mutation-before-the-commit.md` |
| PM-014 | Deleting a folder in the File Browser destroyed images a snapshot still referenced; restore could not materialize them | The versioning-hook audit was scoped by *router* rather than by the table it writes, so a delete endpoint outside `routers/images.py` fired nothing | MITIGATED | `docs/dev/postmortems/PM-014-filesystem-delete-bypassed-the-versioning-hook.md` |
| PM-015 | `GET /%2e%2e/%2e%2e/dataset_manager.db` (or `//etc/passwd`) returned any file on the host with a 200; path traversal / arbitrary file read through the SPA catch-all | An unguarded `FileResponse` over a URL segment in `main.py`, outside the routers every containment sweep enumerated — percent-encoded dots survive client normalization, and `Path / "/abs"` discards the base | MITIGATED | `docs/dev/postmortems/PM-015-spa-catch-all-served-any-file.md` |
| PM-016 | Restoring a snapshot whose source video had since been deleted failed with `FOREIGN KEY constraint failed`; the job failed identically on every retry, leaving that snapshot and its branch permanently un-restorable while burning a pre-restore auto-snapshot each attempt | A column mirrored onto `VersionImageState` *without* its foreign key was written back verbatim; `ondelete="SET NULL"` covers deleting the parent, not updating a child onto a missing one — and the suite runs with SQLite's `foreign_keys` pragma off, so no test could see it | MITIGATED | `docs/dev/postmortems/PM-016-restore-wrote-a-dangling-lineage-fk.md` |
| PM-017 | Renumbering a dataset twice made one image's gallery tile show another's picture and replaced its caption with the other's; every filename was correct, so no naming assertion saw it (`FileExistsError` from `Path.rename` on Windows instead) | A two-phase rename's collision test covered the **image path** while the artifacts a rename moves — `{stem}.webp`, `{stem}.txt` — are keyed on the stem alone, so a cross-extension collision took the direct branch | MITIGATED | `docs/dev/postmortems/PM-017-bulk-rename-clobbered-a-sibling-stem.md` |
| PM-018 | `POST /images/batch/resize` returned 404 "Image not found" and `/images/batch/crop` a 422 naming `x`/`y`; both endpoints had never executed, hiding a missing busy guard, a pre-PM-013 commit ordering and a static job label | A literal path segment declared *after* a parameterized route that matches it — FastAPI matches in declaration order and `image_id: str` accepts `"batch"`, with no fallthrough | MITIGATED | `docs/dev/postmortems/PM-018-batch-routes-shadowed-by-a-path-parameter.md` |
| PM-019 | Every fresh Windows `manage.ps1 setup` on an NVIDIA box died at `[4/7]` with `NativeCommandError` on `pip.exe : WARNING: Skipping torch as it is not installed` — a warning about a no-op, killing the whole install | Redirecting a native command's stderr (`2>$null`) is what wraps it in an ErrorRecord, which file-scope `$ErrorActionPreference = "Stop"` then makes terminating — the redirect was the trigger, not the suppression; third encounter with the trap in one file | MITIGATED | `docs/dev/postmortems/PM-019-redirected-stderr-aborted-setup.md` |
| PM-020 | Stats → Saturation histogram was one `0–10` bar on every dataset; clicking it filtered on bounds matching every row | Bucket edges copied between two scores on different numeric ranges (0–1 vs 0–100); `_bucket` clamps below the first edge instead of erroring | MITIGATED | `docs/dev/postmortems/PM-020-saturation-histogram-scale.md` |
| PM-021 | Deleting a video on Windows 500'd with `PermissionError: [WinError 32] ... being used by another process`; rename had the same exposure. Invisible on POSIX, so CI and the whole suite were green | The app's own `FileResponse` held the file open for a body the browser never finished consuming, and the `<video>` stayed mounted through the mutation; Windows refuses to unlink or rename a file held without `FILE_SHARE_DELETE` | MITIGATED | `docs/dev/postmortems/PM-021-served-file-handle-blocked-windows-delete.md` |
